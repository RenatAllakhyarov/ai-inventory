import AiInventoryPage from "@pages/AiInventoryPage";
import { type ReactElement } from "react";

const Application = (): ReactElement => {
    return <AiInventoryPage />;
};

export default Application;
